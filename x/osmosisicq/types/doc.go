// Package types holds the deprecated Osmosis ICQ type definitions. All generated
// files are now gated behind the "osmosis_removed" build tag so they are excluded
// from the default build.
package types
