syntax = "proto3";

package bitora.token.v1;

import "amino/amino.proto";
import "bitora/token/v1/params.proto";
import "cosmos/msg/v1/msg.proto";
import "cosmos_proto/cosmos.proto";
import "gogoproto/gogo.proto";

option go_package = "bitora/x/token/types";

// Msg defines the Msg service.
service Msg {
  option (cosmos.msg.v1.service) = true;
  
  // UpdateParams defines a (governance) operation for updating the module
  // parameters. The authority defaults to the x/gov module account.
  rpc UpdateParams (MsgUpdateParams) returns (MsgUpdateParamsResponse);
  
  // Mint defines the Mint RPC.
  rpc Mint (MsgMint) returns (MsgMintResponse);
  
  // Burn defines the Burn RPC.
  rpc Burn (MsgBurn) returns (MsgBurnResponse);
  
  // FinalizeToken defines the FinalizeToken RPC.
  rpc FinalizeToken (MsgFinalizeToken) returns (MsgFinalizeTokenResponse);
  
  // CreateToken defines the CreateToken RPC.
  rpc CreateToken (MsgCreateToken) returns (MsgCreateTokenResponse);
}
// MsgUpdateParams is the Msg/UpdateParams request type.
message MsgUpdateParams {
  option (cosmos.msg.v1.signer) =                      "authority";
  option           (amino.name) = "bitora/x/token/MsgUpdateParams";
  
  // authority is the address that controls the module (defaults to x/gov unless overwritten).
  string authority = 1 [(cosmos_proto.scalar) = "cosmos.AddressString"];

  // params defines the module parameters to update.
  
  // NOTE: All parameters must be supplied.
  Params params = 2 [(gogoproto.nullable) = false, (amino.dont_omitempty) = true];
}

// MsgUpdateParamsResponse defines the response structure for executing a
// MsgUpdateParams message.
message MsgUpdateParamsResponse {}

// MsgMint defines the MsgMint message.
message MsgMint {
  option (cosmos.msg.v1.signer) = "creator";
  string creator = 1 [(cosmos_proto.scalar) = "cosmos.AddressString"];
  string to      = 2;
  uint64 amount  = 3;
}

// MsgMintResponse defines the MsgMintResponse message.
message MsgMintResponse {}

// MsgBurn defines the MsgBurn message.
message MsgBurn {
  option (cosmos.msg.v1.signer) = "creator";
  string creator = 1 [(cosmos_proto.scalar) = "cosmos.AddressString"];
  string to      = 2;
  uint64 amount  = 3;
}

// MsgBurnResponse defines the MsgBurnResponse message.
message MsgBurnResponse {}

// MsgFinalizeToken defines the MsgFinalizeToken message.
message MsgFinalizeToken {
  option (cosmos.msg.v1.signer) = "creator";
  string creator     = 1 [(cosmos_proto.scalar) = "cosmos.AddressString"];
  string denom       = 2;
  bool   make_public = 3;
}

// MsgFinalizeTokenResponse defines the MsgFinalizeTokenResponse message.
message MsgFinalizeTokenResponse {}

// MsgCreateToken defines the MsgCreateToken message.
message MsgCreateToken {
  option (cosmos.msg.v1.signer) = "creator";
  string creator        =  1 [(cosmos_proto.scalar) = "cosmos.AddressString"];
  string name           =  2;
  string symbol         =  3;
  uint32 decimals       =  4;
  uint64 initial_supply =  5;
  uint64 max_supply     =  6;
  bool   mintable       =  7;
  bool   pos_compatible =  8;
  string icon_uri       =  9;
  string description    = 10;
}

// MsgCreateTokenResponse defines the MsgCreateTokenResponse message.
message MsgCreateTokenResponse {}

